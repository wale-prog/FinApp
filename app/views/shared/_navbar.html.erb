<nav class="navbar navibar">
  <div class="container-fluid d-flex mx-3" style="align-items:center">
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse"  id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <% if user_signed_in? %>
          <li class="nav-item">
            <%= link_to current_user.name, user_category_index_path(current_user), class: "nav-link" %>  
          </li>
          <li class="nav-item">
            <%= link_to 'Create category', new_user_category_path(current_user), class: "nav-link" %>  
            </li>
          </li>        
        <% end %>      
      </ul>
      <div class= "d-flex">
        <% if user_signed_in? %>      
        <ul class="navbar-nav me-auto mb-2 mb-lg-0" style= 'align-items:center'>
          <li class="nav-item">
             <%= link_to 'Edit Profile', edit_user_registration_path, class: "nav-link" %>  
          </li>
          <li class="nav-item">
            <%= button_to 'Log Out', destroy_user_session_path, :method => :delete, class: "btn btn-link nav-link" %>  
          </li>
        </ul>
        <% end %>          
        <form class="d-flex" role="search">
          <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
          <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
      </div>      
    </div>
    <div>
      <% if request.fullpath == user_category_index_path(current_user)  %>
        <p>Tracker Category</p>
      <% elsif request.fullpath == new_user_category_path(current_user) %>
        <p>Create Category</p>
      <% end %>
    </div>
    <p class= "finapp">FinApp</p>
  </div>
</nav>
